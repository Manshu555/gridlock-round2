# ---- Core data / ML ----
pandas>=2.0
pyarrow>=14.0        # parquet engine (required to read pipeline outputs)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
h3>=4.0
lightgbm>=4.0          # forecasting (pipeline falls back to sklearn GBDT if unavailable)
shap>=0.44            # model explanations (optional at runtime)

# ---- GIS (optional; pipeline degrades gracefully if missing) ----
geopandas>=0.14
shapely>=2.0
osmnx>=1.9            # OSM road enrichment (optional; class-based defaults used offline)
pysal>=24.1          # esda/libpysal for reference Gi*; a NumPy implementation ships in-repo

# ---- Backend API ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
geoalchemy2>=0.14
psycopg2-binary>=2.9

# ---- Tooling / tests ----
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
ruff>=0.4
mypy>=1.9
pre-commit>=3.7
